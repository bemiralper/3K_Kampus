import TanimlarClient from "./TanimlarClient";

/**
 * /finans/tanimlar → Finans Tanımları (Ödeme Yöntemleri + Mali Hesaplar)
 */
export default function TanimlarPage() {
  return <TanimlarClient />;
}
