import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sistem Yönetimi | 3K Kampüs',
};

export default function SistemYonetimiLayout({ children }: { children: React.ReactNode }) {
  return children;
}
