Django==4.2.9
psycopg[binary]>=3.1.0
gunicorn>=21.2.0
djangorestframework==3.14.0
django-cors-headers==4.3.1
python-decouple==3.8
Pillow>=10.0.0
python-dateutil>=2.8.0
httpx>=0.27.0
cryptography>=42.0.0
boto3>=1.34.0

# Opsiyonel — Celery + Redis (Faz 5 async kuyruk; boş CELERY_BROKER_URL ile atlanır)
celery>=5.3.0
redis>=5.0.0

# Headless PDF — ödev sonuç raporu print route render
playwright>=1.40.0

# PDF fallback (plan metin PDF)
reportlab>=4.0.0

# Finans XLSX export (Faz 4)
openpyxl>=3.1.0

# Sistem Yönetimi — host metrikleri
psutil>=5.9.0
