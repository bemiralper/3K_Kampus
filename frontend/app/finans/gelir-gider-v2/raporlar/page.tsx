import GGProvider from "../GGProvider";
import RaporClient from "../RaporClient";

export default function GelirGiderRaporlarPage() {
  return (
    <GGProvider>
      <RaporClient />
    </GGProvider>
  );
}
