import MetaSablonlarClient from "./MetaSablonlarClient";

export const metadata = {
  title: "Meta Şablonları — 3K Kampüs",
};

export default function MetaSablonlarPage() {
  return <MetaSablonlarClient />;
}
